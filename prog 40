#include <stdio.h>

int cube(int num) {
    return num * num * num;
}

int main() {
    int num;
    printf("Enter any number: ");
    scanf("%d", &num);
    printf("Cube of %d is %d", num, cube(num));
    return 0;
}
