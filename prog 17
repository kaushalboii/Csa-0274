#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main() {
    char str[100];
    int i;

    printf("Enter a string: ");
    scanf("%[^\n]", str);

    for(i = 0; str[i]; i++){
        str[i] = tolower(str[i]);
    }

    printf("Lowercase string is: %s\n", str);

    return 0;
}
